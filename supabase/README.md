# Supabase Structure

This directory reserves project space for future Supabase work.

- `migrations/` will hold reviewed SQL migrations and RLS policies.
- `functions/` will hold Edge Functions for authorized jobs such as sync or scoring support.

This scaffold intentionally contains no migrations, no Edge Function implementation, and no configured remote project.
