import { LoginStage } from "@/components/auth/login-stage";

export default function LoginPage() {
  // This route follows the first Stitch login audit pass without real auth yet.
  return <LoginStage />;
}
